#include "main.h"
/**
 * _abs - computes the absoulute value of an integer
 * @n: the int to be check
 * Return: the absoulute value of int
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
